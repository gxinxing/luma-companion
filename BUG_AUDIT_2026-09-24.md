# LumaCompanion 手机端 App — 第三轮系统性 Bug 排查与修复

日期：2026-09-24 03:20–04:05（Asia/Shanghai）
范围：`luma-companion/` iOS App 全量源码（15 个 Swift 文件，约 3 000 行）

前两轮已修 14 + 31 项（见 `README.md` / `HANDOFF.md`）。本轮是在那之上的独立再排查，
**不与已修项重复**。

---

## 一、先回答定位所需的信息（这轮我核实到的）

### 技术栈与项目结构

| 项 | 事实 |
|---|---|
| 语言 / UI | Swift 5.9 + SwiftUI（100%，无 UIKit VC、无 storyboard） |
| 并发模型 | `@MainActor` ObservableObject + `async/await` + `CheckedContinuation` |
| 部署目标 | iOS 17.0（deployment target），只支持 iPhone（`TARGETED_DEVICE_FAMILY=1`） |
| 关键系统框架 | CoreBluetooth、Network（`NWConnection`/`NWListener`）、AVFoundation、Photos、NetworkExtension |
| 协议层 | 全部来自本地 Rust 静态库 `luma-core` 经 UniFFI 生成的 Swift 包 `LumaCore`（App 内不解析任何协议字节） |
| 工程生成 | `xcodegen`（`project.yml` → `LumaCompanion.xcodeproj`）；**新增源文件后必须重跑 xcodegen**，否则不进工程 |

```
luma-companion/LumaCompanion/
├── LumaCompanionApp.swift          @main + 四个 tab
├── Features/
│   ├── Capture/CaptureView.swift       拍摄：BLE 回传的预览 + 双手势卡片
│   ├── Live/LiveScreen.swift           实时画面：RTSP/RTP → AVSampleBufferDisplayLayer
│   ├── Memories/MemoriesScreen.swift   眼镜相册：0x39 开热点 → HTTP 文件 API
│   └── Device/DeviceView.swift         设备信息 + 蜂群上报入口
├── Services/
│   ├── GlassesLink.swift               CoreBluetooth 唯一所有者（连接/写 FIFO/重连窗口）
│   ├── GlassesWiFi.swift               NEHotspotConfiguration + TCP 探活
│   ├── LiveStreamSession.swift         RTSP 会话 + RTP 接收 + VideoPipeline + TcpTransport
│   ├── FileApiClient.swift             眼镜侧四个 HTTP GET 的封装
│   └── LiveAudioPlayer.swift           AAC → AVAudioConverter → AVAudioEngine
└── Support/
    ├── SwarmLink.swift                 图像特征提取 + 上报 aria-swarm
    ├── CaptureStore.swift / SwipeableCaptureCard.swift / Theme.swift
    └── ImageDecodeCache.swift          【本轮新增】
```

### 目标机型与系统版本

- 目标：**iPhone 15 Pro / iOS 26.5**（与 `README.md` 记录的验证机型一致），部署下限 iOS 17.0。
- 构建环境：Xcode 26.6 (Build 17F113)，Swift 6.3.3，`iPhoneSimulator26.5` / `iPhoneOS26.5` SDK。
- 硬件依赖：**链路首 today 需要眼镜在线**（E06-00F7），涉及 BLE/热点/RTSP 的问题只能真机复现。

### 关于 bug 表现、复现步骤、日志/崩溃堆栈

本次是**静态排查**，不是 bug 工单转述：没有用户报的复现脚本、也没有崩溃堆栈可供分析。
所以每条问题我都写成了「代码里的触发路径 + 可执行的复现动作 + 观察点」，而不是表演式的猜测。
日志来源：`xcodebuild` / `swiftc` 诊断输出，以及 `aria-swarm/src/runtime.mjs`（核实服务端契约）。

### 本轮的验证方式与其上限（重要）

| 验证手段 | 结果 |
|---|---|
| `xcrun swiftc -typecheck`，**iphonesimulator26.5 / arm64-apple-ios17.0-simulator** | ✅ 15/15 文件，**0 error 0 warning**（exit 0） |
| `xcrun swiftc -typecheck`，**iphoneos26.5 / arm64-apple-ios17.0**（真机） | ✅ 15/15 文件，**0 error 0 warning**（exit 0） |
| `xcodegen generate` 重新生成工程 | ✅ 新增 `ImageDecodeCache.swift` 已进入 `project.pbxproj`（4 处引用） |
| `xcodebuild` 完整构建（含链接） | ✅ 后续已在当前工作区完成模拟器与 Ricky 付费签名真机构建；当前命令与签名事实见 `HANDOFF.md` |

---

## 二、问题清单（按严重程度分级）

共 **18 项**，其中已修 18 项。没有发现「必崩」型的 force-unwrap / 越界崩溃，
最高等级是**链路级功能失效**——它们在演示现场的表现比崩溃更糟：不报错、不退出，只是再也不通了。

### P0 演示致命（链路静默死亡）

| # | 问题 | 文件 |
|---|---|---|
| L3-1 | 直连失败后外设引用残留 → 后续广播全被屏蔽 → **永久失联** | `GlassesLink.swift` |
| L3-2 | AA13 的 ACK 不来时 `write()` 永久挂起 → **Live/记忆永远停在第一步** | `GlassesLink.swift` |

### P1 功能失效 / 资源泄漏

| # | 问题 | 文件 |
|---|---|---|
| L3-3 | Live 拉起失败不回收热点（缺 0x44 / leave）→ **眼镜持续 SoftAP 耗电** | `LiveStreamSession.swift` |
| L3-4 | 记忆 bring-up 失败同样不回收热点 | `MemoriesScreen.swift` |
| L3-5 | Live 停止后 `isStreaming` 仍为真 → **UI 假装还在直播、按钮失灵** | `LiveStreamSession.swift` / `LiveScreen.swift` |
| L3-6 | 记忆 tab 在「眼镜后连上」的情况下永不重试 | `MemoriesScreen.swift` |
| L3-7 | 来电/横幅通知打断音频后静默失效、界面还显示正常状态 | `LiveAudioPlayer.swift` |

### P2 数据正确性 / 性能

| # | 问题 | 文件 |
|---|---|---|
| L3-8 | 感知哈希 `%016x` 接 UInt64 → 只印低 32 位，**一半信息静默丢失** | `SwarmLink.swift` |
| L3-9 | 场次已结束仍向旧 `runId` 上报 → 200 OK + 「已上报」**假成功** | `SwarmLink.swift` |
| L3-10 | 三处视图在 `body` 里同步解码 JPEG → **滚动/手势卡顿** | `CaptureView` / `SwipeableCaptureCard` / `MemoriesScreen` |
| L3-11 | 25 fps 每帧写 `@Published stats` → SwiftUI 每秒重算 25 次 body | `LiveStreamSession.swift` |
| L3-12 | App 退后台不收尾 → 眼镜热点空转耗电 | `LiveScreen` / `MemoriesScreen` |
| L3-13 | 中台地址缺 scheme / 带尾斜杠 → 直接 `badURL` 或拼出畸形路径 | `SwarmLink.swift` |
| L3-14 | 上报失败只报 HTTP 码，丢掉服务端回复体 → 401/409 无法自诊断 | `SwarmLink.swift` |

### P3 代码质量与系统版本兼容

| # | 问题 | 文件 |
|---|---|---|
| L3-15 | `ledText` 里 `value!` 强制解包（当前排布安全，改动 switch 即崩） | `DeviceView.swift` |
| L3-16 | `onChange(of:perform:)` 单参形式，iOS 17 起已废弃（3 处） | `CaptureView` / `LiveScreen` / `MemoriesScreen` |
| L3-17 | `Text("\($0)")` 走 LocalizedStringKey → 设备页显示英文 `portrait`/`landscape` | `DeviceView.swift` |
| L3-18 | 上报用 `URLSession.shared`，无超时上限 → 现场网络下按钮无限转圈 | `SwarmLink.swift` |

---

## 三、逐项：定位过程 → 根因 → 修复 → 验证

### L3-1 【P0】直连失败后，App 再也发现不到眼镜

**定位过程**
顺着「连接失败后能不能自己回来」这条线追 `GlassesLink` 的状态机，把 `peripheral` 的每一次
赋值与置空列出来，发现它是一个**单向开关**：

```
connect(p)              peripheral = p        ← 唯一赋值点
didDisconnectPeripheral resetConnectionState() → nil
disconnect()            resetConnectionState() → nil
didFailToConnect        ——什么都不做——        ← 漏了
```

而 `didDiscover` 的第一条判据是 `guard self.peripheral == nil, !self.phase.isConnected`，
注释写得很清楚：「一轮只认一个候选」。

**根因**
`didFailToConnect` 只清了记住的 UUID（`rememberedIDDefaultsKey`）、没放开 `peripheral`。
于是后续 30 轮重连全部走到 `scan()` → 扫到了 → 被这条 guard 静默丢弃 → 5 分钟窗口烧完 →
报「持续 5 分钟未找到眼镜」。期间眼镜其实一直在正常广播。

**复现步骤**
1. App 启动（自动 áp dụng 记住的 ID 直连）或冷启动第二次；
2. 让直连失败——最容易触发的办法是眼镜刚关机再开机（≈10 s 重连间隔内必现）；
3. 观察：状态永远是「正在寻找眼镜…」，5 分钟后报超时；期间任何其他 BLE 工具都能扫到这台眼镜。

**修复**（`GlassesLink.swift`）
抽取 `detachPeripheral()`（置空 `peripheral`、`writeCharacteristic`、放行所有等待的写），
在 `didFailToConnect` 里、`phase = .failed` 之前调用；并用 `guard self.peripheral === peripheral`
确认清的就是当前这一台，避免误伤已经连上的另一路。
跨会话记忆（`rememberedName`）刻意不清，名称回退扫描照旧可用。

**验证**
- 编译：两套 SDK 0 诊断。
- 真机：按上面复现步骤 1–3，修复后第 2 步失败后的下一轮（≤10 s）应扫到并连上，
  日志/状态应从 `connecting` 走到 `connected` 而不是烧完 30 轮。
- 回归：正常路径（首次连接成功、GATT 校验失败后的 `cancelPeripheralConnection`）不受影响——
  那两条路径本来就会走到 `didDisconnectPeripheral`。

---

### L3-2 【P0】BLE 写没有确认超时，整个流程可以永久挂在第一步

**定位过程**
`write()` 用 `withCheckedContinuation` 等 ATT 确认。能让这个 continuation 落地的只有两处：
`didWriteValueFor`（收到 ACK）和 `failAllWrites()`（连接断开）。**第三条路径没写**：
ACK 既不返回、连接也没断。

**根因**
`write("openWifi(live)")` 是「实时画面」和「记忆」两页的第一行代码。它一旦挂起，
`awaitSSID()` 的 15 秒超时、`waitForHost` 的 75 秒窗口统统轮不到执行——整条链路**没有时间上限**。
UI 表现是永远停在「正在打开眼镜热点」，而此时 `LiveStreamSession.task != nil`，
连「重试」按钮都被 `guard task == nil` 挡住。这是一个没有任何出口的死锁。

**修复**（`GlassesLink.swift`）
加一道 8 秒看门狗（与已有的 `ResumeOnce` 一样保持代码风格：`arm/disarm` + `@MainActor Task`）：

- 追加 waiter 时 `armAckWatchdog()`；
- 到点 `ackTimedOut()` → `failAllWrites()` → **放行等待者**，流程继续走到下一步的真实超时并报错；
- 收的到 ACK 就 `disarmAckWatchdogIfIdle()`——注意这里是 `IfIdle`：AA13 是 Live 和记忆共用的，
  先返回的那一路不能把还在等待的另一路的看门狗一起掐掉（这是我第一版写错、自己复查时发现并修掉的）。

**验证**
- 编译：两套 SDK 0 诊断。
- 逻辑复查：`send()`（fire-and-forget）与 `write()` 混合时 FIFO 顺序不被破坏；
  并发两路写时两路都有超时保护。
- 真机：眼镜开机后立刻点「实时」，或在写入过程中关闭眼镜再立刻开机；修复后最迟 8 s 应看到
  「等待网络名称」失败提示而不是永久停在第一步。

---

### L3-3 / L3-4 【P1】拉起失败不回收热点，眼镜一直开着 SoftAP 耗电

**定位过程**
代码注释明确写了工程约束：「任何退出路径都写 `0x44`（否则眼镜停在 SoftAP 模式持续耗电）」。
把三个 tab 的退出路径全部枚举后，发现 `0x44` 只挂在**用户主动退出**上
（`LiveStreamSession.stop()` / `MemoriesModel.finish()`），失败路径一个都没有。

**根因**
`run()` 的 `catch { failure = …; status = "failed" }` 里没有任何资源回收。而失败后的典型用户行为
恰恰是直接切走 tab 或连续点重试——于是 `0x67`/`0x39` 已经把热点拉起来了，却再没人关。
UDP 端口也一样：`teardownResources()` 只在 `start()` 里被调用（所以后来的重试靠这个躲过了
port in use），但在「失败后不再重试」的情况下端口会一直占到下一次进入。
演示日的眼镜电量，大部分可能就丢在这。

**修复**
- `LiveStreamSession.abandon()`：`teardownResources()`（放开 UDP/TCP）+ 写 `0x44` + `GlassesWiFi.leave()`；
- `MemoriesModel.abandon()`：同样 + `session.finishTasksAndInvalidate()`；
- 两处都只在 `catch` 且 `Task.isCancelled == false` 时调用——
  被取消意味着是 `stop()`/`finish()` 那一路，它们已经在做同样的事，再写一遍只是让眼镜收到两条 `0x44`。

**验证**
- 编译：两套 SDK 0 诊断。
- 真机：进「记忆」tab，在「等待网络名称」阶段关掉眼镜（制造失败）；修复后眼镜应退出热点
  （可用另一台手机搜不到该 SSID 验证），而不是一直广播。

---

### L3-5 【P1】Live 停止后界面假装还在直播，按钮失灵

**定位过程**
读 `stop()` 时发现注释里写着「step 故意保留」。再看 `isStreaming`：

```swift
var isStreaming: Bool { step == .streaming && failure == nil }
```

只看了两个条件，而 `finished` 恰恰是 `stop()` 唯一会置位的东西。

**根因**
从 streaming 状态点「停止」后：`step` 仍是 `.streaming`、`failure` 仍是 nil
→ `isStreaming == true` → 右上角统计浮层不撤、副标题写着「实时画面 · 1600×1200」，
按钮仍显示「停止」，但因为 `stop()` 开头 `guard !finished else { return }`，**按下去什么都不做**。
用户唯一的出路是重进 tab。

**修复**
- `isStreaming` 加上 `&& !finished`；
- `LiveScreen` 的按钮分支从「二元」改成四态：**停止 / 重新开始 / 取消 / 状态条**。
  `finished` 时给 `.borderedProminent` 的「重新开始」，直接走已有的 `start()` 自愈路径。

**验证**
- 编译：两套 SDK 0 诊断。
- 界面：iOS Simulator 或真机跑一次，从直播中点「停止」→ 应立刻切到 `重新开始` 且统计浮层消失。

---

### L3-6 【P1】记忆 tab 在「眼镜后来才连上」时永不重试

**定位过程**
对比两个 tab 的启动钩子：`LiveScreen` 有 `.onChange(of: link.phase)`，`MemoriesScreen` 没有。
而 `MemoriesModel.start()` 开头是 `guard link.phase.isConnected else { failure = "眼镜未连接…"; return }`。

**根因**
用户在眼镜连上之前切进记忆 tab → 拿到一句静态错误 → 此后再也没人触发重试，
必须手动离开再切回来。`start()` 里也没清 `failure`，所以「重试」按钮在这种路径下会保留上一次的失败文案。

**修复**
- `MemoriesModel.retryIfIdle()`：`finished / task != nil / 正在浏览` 三种情况不动，其余调 `start()`；
- `start()` 里在通过连接检查后 `failure = nil`；
- `MemoriesScreen` 补上 `.onChange(of: link.phase)`（同时把 `link` 声明为 `@EnvironmentObject`，
  之前只有 `init` 入参，拿不到 phase 变化）。

**验证**
- 编译：两套 SDK 0 诊断。
- 真机：蓝牙关着进记忆 tab（显示「眼镜未连接」）→ 开蓝牙等连上 → 应自动开始拉热点，不需重进 tab。

---

### L3-7 【P1 兼容性】来电/通知打断音频后静默失效

**定位过程**
`LiveAudioPlayer` 只实现了 `start/stop/play`，订阅了 `AVAudioSession` 的 category，
但没有订阅 `interruptionNotification`。

**根因**
来电、某些横幅提醒、其他 App 抢占 audio session，都会让系统直接停掉 `AVAudioEngine`。
这一侧完全无感：不抛错、不回调，`audioState` 还写着「AAC-LC 16000 Hz, 1 ch」这种正常文案，
而声音从此消失。演示现场来一条微信横幅就够触发。

**修复**
- 新增 `interrupted` 状态 + `AVAudioSession.interruptionNotification` 观察器；
- `.began`：置 `interrupted = true`（`play()` 直接丢帧，不在停掉的引擎上堆 buffer）+ 如实改一行状态；
- `.ended`：重新 `setActive(true)` + `engine.start()` + `player.play()`，成功/失败都写回 `audioState`；
- `stop()` 里注销观察器；
- 通过新增的 `onStateChange` 回调接到 `LiveStreamSession.audioState`（回调在音频队列上，
  会话侧用既有的 `Task { @MainActor in }` 落地，与其他回调写法一致）。

**验证**
- 编译：两套 SDK 0 诊断。
- 真机：直播出声时切控制中心播放音乐（或用另一 App 抢占），应看到状态变为「被系统中断…」，
  中断结束后变为「音频已恢复」并真的出声。

---

### L3-8 【P2 数据正确性】感知哈希丢掉一半位数

**定位过程**
`SwarmLink.averageHash` 生成 64 位 bit 串，最后一行：

```swift
return String(format: "%016x", UInt64(bits, radix: 2) ?? 0)
```

`%x` 的长度修饰符是 `unsigned int`（32 位），实际入参却是 `UInt64`。

**根因**
64 位平台上 `UInt64` 与 `%x` 不匹配，格式化只读到低 32 位并补零到 16 位。
结果是 dedup_id 的有效长度只有一半，碰撞概率凭空涨 2³² 倍——而且**完全静默**，不报错。

**修复**
改成 `%016llx`（`UInt64` 对应的长度修饰符）。

**验证**
单元层面：`bits` 取全 1（`UInt64.max`）时，修复前应输出 `00000000ffffffff`（错），
修复后输出 `ffffffffffffffff`。可用一小段 Swift 脚本核对。

---

### L3-9 【P2 假成功】向已结束的场次上报，照样显示「已上报」

**定位过程**
`currentRunID` 的文档注释写着「status=running 才有效」，但实现里只取 `runId`。
对照 `aria-swarm/src/runtime.mjs` 核实：run 对象确实有 `status` 字段
（`running` / `completed` / `superseded`），而 `snapshot()` 会返回 `runId: run.id`。

**根因**
场次结束后 `snapshot` 仍带旧 `runId`。照它上报会拿到 HTTP 200，UI 显示「已上报 xxxx · 强度 0.42」，
而音乐侧什么都不会发生。这正是最容易被当成成功的那类失败——项目的既有纪律里明确要求
「不得静默把失败伪装成成功」。

**修复**
`status` 字段存在且不是 `running` 时，抛 `noRun` 并显式说明当前状态。
「字段存在才校验」是有意的向后兼容：不假设所有部署都一定会带 `status`。

**验证**
对着一场已停止的演出点「上传最近一张」，应报「当前场次状态为 completed（需要 running）」
而不是显示上报成功。

---

### L3-10 【P2 性能】三处视图在 `body` 里同步解码 JPEG

**定位过程**
`CaptureView`（`UIImage(data:)`）、`SwipeableCaptureCard`（`UIImage(contentsOfFile:)`）、
`MemoriesScreen.Cell`/`MemoryViewer`（`UIImage(data:)`）都直接在 `body` 里解图。

**根因**
`body` 不是只跑一次的地方：StatePill 的电量/阶段变化、Toast 动画、卡片拖动的每一帧位移
都会触发重算，于是同一张图被反复解码，全部在主线程上。直接表现是出 Toast 时抖一下、
网格滚动发涩、卡片拖动不跟手。

**修复**
新增 `Support/ImageDecodeCache.swift`，保持项目「小而专注的工具 + 头部注释说明为什么」的风格：
`NSCache` 缓存 + `Task.detached` 后台解码/读盘，`countLimit = 240`，
并订阅 `UIApplication.didReceiveMemoryWarningNotification` 整批清空（缩略图可再生，不该顶峰值）。
对外提供 `CachedImage` / `CachedFileImage` 两个 View（用 `.task(id:)` 驱动），三处以最小改动接入。
删除策略调整的依据是同名文件复用风险。
删除本地文件时同步 `ImageDecodeCache.invalidate(_:)`，避免下一个同名文件显示上一张的像素。

**验证**
- 编译：两套 SDK 0 诊断 + `xcodegen` 后文件已进 `project.pbxproj`。
- 界面：记忆页网格快速滚动、拍摄页连点 Toast、最近拍摄卡片左右拖动，帧率应明显顺滑。

---

### L3-11 【P2 性能】25 fps 每帧写 `@Published`

**定位过程**
`pipeline.onSample` 每帧 `self.apply(snapshot)` → 写 `stats`（`@Published`）→
`StatsOverlay` 用 `@ObservedObject` 订阅 → SwiftUI 每秒重算 25 次 body；叠加/> 转写这些本来就在 CPU 主线程上。

**根因**
统计数字是人读的，不需要 40 ms 的刷新率；样本是 GPU 用的，必须一帧不落。原先两者被绑在同一个闭包里。

**修复**
保留 `pendingSnapshot` 快照，统计以 **2 Hz** 落盘（`flushStatsIfNeeded()`，间隔 0.5 s），
`frames.send(sample)` 不等、仍旧一帧一发。撤销 frames Some 的额外延迟。

**验证**：直播时右下角 fps 读数从「每秒跳 25 次」变成「每 0.5 秒跳一次」，画面不应有可感知延迟变化。

---

### L3-12 【P2】App 退后台不收尾

**根因**
只有 `onDisappear` 会触发收尾。App 退后台时 iOS 会挂起 UDP/TCP，回来链路已是死的，
而眼镜在这段时间里一直开着热点、摄像头持续供电。

**修复**
两个 tab 都订阅 `UIApplication.didEnterBackgroundNotification`，
走与离开 tab 完全相同的收尾路径（记忆页额外保留「查看器开着时不拆会话」的既有保护）。

**注意**：这里刻意用 `didEnterBackground` 而不是 `willResignActive`——
后者在拉下通知éis时也会触发，会把正在演示的直播打断。

**验证**：直播中按 Home 键退后台，眼镜应退出热点（同 L3-3 的验证办法）。

---

### L3-13 / L3-14 / L3-18 【P2】上报通道的三个健壮性问题

| 问题 | 修复 |
|---|---|
| 中台地址缺 scheme（`ytd.rickyke.com`）、结尾带 `/`、首尾空格 | 新增 `normalizedBaseURL(_:)`：trim → 补 `https://` → 去尾部斜杠 → 空则回落默认地址；`currentRunID` 与 `sendCapture` 都过一遍 |
| 上报失败只报 HTTP 码 | 把服务端回复体前 160 字符拼进错误信息（401/409 一般写着令牌无效、场次未运行等真实原因） |
| `URLSession.shared` 无超时 | `request.timeoutInterval = 15`。现场网络、或手机还挂在眼镜那个没有外网的 SoftAP 上时，这会表现为按钮上的菊花转很久 |

**验证**（这三个正好对应今天缺令牌的调试场景）：故意把令牌清空再上报，
应立刻给出「上报被拒（HTTP 401）：<服务端原文>」而不是转半天圈后吐一个裸的 401。

---

### L3-15 / L3-16 / L3-17 【P3】代码质量与 iOS 17 兼容

| # | 修复 |
|---|---|
| L3-15 | `ledText` 的 `default: "未知 (\(value!))"` 改写成 `case let .some(raw): "未知 (\(raw))"`。眼下排布保证了非空、不会崩，但只要有人改动这条 switch，`!` 就会把整个设备页变成崩溃——把「暂不崩」换成「永远不会崩」 |
| L3-16 | 三处 `onChange(of:perform:)` 单参形式改成 iOS 17 推荐形式（零参 / 两参），语义完全一致，清掉废弃告警 |
| L3-17 | `Text(states.orientation.map { "\($0)" } ?? "…")` 在 `LocalizedStringKey` 里插的是调试描述，屏幕上直接显示英文 case 名（`portrait` / `landscape`）。改成 `orientationText()` 输出「竖拍 / 横拍」（取值已从 `LumaCore` 生成的枚举核实：只有 `.portrait` / `.landscape` 两例） |

---

## 四、改动清单

| 文件 | 改动 |
|---|---|
| `Services/GlassesLink.swift` | `detachPeripheral()`；`didFailToConnect` 清引用；写 ACK 看门狗（8 s）+ `disarmAckWatchdogIfIdle()` |
| `Services/LiveStreamSession.swift` | `isStreaming` 收 `finished`；`abandon()`（失败路径回收热点 + 放开端口）；失败 catch 分支；统计节流 2 Hz；接 `onStateChange` |
| `Services/LiveAudioPlayer.swift` | 中断观察 + `interrupted` 状态 + `onStateChange` 回调 + `stop()` 注销观察器 |
| `Features/Live/LiveScreen.swift` | 四态按钮（含「重新开始」）；`didEnterBackground` 收尾；`onChange` 新签名 |
| `Features/Memories/MemoriesScreen.swift` | `abandon()`；`retryIfIdle()`；`start()` 清 `failure`；`link` 改为 `@EnvironmentObject`；`onChange` + `didEnterBackground` 收尾；接入 `CachedImage` |
| `Features/Capture/CaptureView.swift` | 接入 `CachedImage`；删除时同步失效缓存；`onChange` 新签名 |
| `Support/SwipeableCaptureCard.swift` | 接入 `CachedFileImage`（消除主线程磁盘读 + 解码） |
| `Support/SwarmLink.swift` | `%016llx`；`normalizedBaseURL()`；`status == running` 校验；15 s 超时；失败携带服务端回复体 |
| `Support/ImageDecodeCache.swift` | **新增**：NSCache + 后台解码 + `CachedImage` / `CachedFileImage` |
| `Features/Device/DeviceView.swift` | 去掉 `value!`；`orientationText()` 中文翻译；`import LumaCore` |

未改动 `project.yml`、Info.plist、entitlements，也没有引入任何第三方依赖。

---

## 五、遗留风险与下一步

1. **完整构建尚未跑过** —— 受本机 `sandbox-exec` / TCC 限制，本会话只能做到「两个 SDK 全量类型检查干净」。
   请在自己的 Terminal 跑一次第二节末尾的 `xcodebuild` 命令（含链接），再装真机。
2. **L3-1 / L3-2 / L3-3 / L3-7 需要眼镜在线才能真实验证** —— 我在报告里给每条写了具体的复现动作与观察点，
   戴机上电后按顺序过一遍即可。
3. **并发写的看门狗是共用的**：两路同时写 AA13 时，超时会同时放行两路（先到期的那一次触发）。
   这是刻意的取舍——比其中一路永久挂起好；若要更精细，得改成每槽一个计时器，复杂度不值。
4. **`CacheImage` 首次解码有一段极短的空窗**：拍摄页第一次出现新预览时可能闪一下黑。
   之后 `# State` 会保留上一张直到新的解码完成（不是 nil），所以只有首张有这个现象。
5. **建议顺手删掉**：工作区根目录下那份疑似误建的嵌套副本 `./EvoTavern 进化酒馆黑客松/aria-swarm`
   （昨天已标记，仍未清理；多人多会话共用同一个工作树时容易改错地方）。
