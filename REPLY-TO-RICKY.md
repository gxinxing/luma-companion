# 回 Ricky：做（附三条实测，其中一条建议你先花 10 分钟确认）

> 2026-09-24 03:50 写。他在等一个"做"字，下面是判断 + 支撑证据 + 可直接粘贴的回复。

## 判断

**做。** 三条都做，方向对——"一次刺激 → 一只蜂 → 一处改变 → 歌变了"这个闭环确实是现在最大的缺口。

但我刚在生产上做了个对照实验，有一条**可能影响你排期**的事实，见下面第 3 条。

## 三条实测（都是生产真数据，不是推断）

### 1. 你的依赖假设成立，而且比你想的方便

`music.traces[]` 里有**独立的 `beeId` 字段**，不用去解析 `observationId`。音符侧 `cause[]` 里有 `decisionId`，两者直接 join 就行：

```json
// music.traces[0]
{"id":"trace:decision:670b…:2","beeId":37,"atBeat":32,"strength":0.6,
 "decisionId":"decision:670b…:2","roundId":"round:0:37",
 "observationId":"observation:g37:rround:0:37:b37:n2"}

// music.phrase.notes[].cause[]
{"type":"decision","decisionId":"decision:670b…:0","roundId":"round:0:37",
 "observationId":"…b35:n0","stimulusIds":["audience:fa114225-…"]}
```

⚠️ 两个坑：
- `observationId` 里有拼写重复的 `rround:0:37`（**双 r**）。别解析它，直接用 `beeId` + `decisionId`。
- 丝带的配色不用另算：`cause.type` 就是 `seed` / `decision`，**灰 / 青白**直接靠它判；"继承来的钴蓝"可以用 roundId 早于本轮来判断。

### 2. 没有刺激，蜂一只都不动

我打刺激之前，演出已经跑了 **35 轮**，`decisions: 0`、`modelCalls: 0`、`traces` 空、26 个音符的 cause 全是 `seed`。打了一个观众刺激之后：`decisions` 0→5、`modelCalls` 0→5、`traces` 出现 2 条、音符 cause 第一次出现 `decision`。

### 3. 🔴 但蜂"决策了"不等于"改了"——这条建议你先确认

那一轮的 metrics 是：`decisions: 5`、`noChange: 5`、`appliedBees: 0`。

**蜂决策了 5 次，5 次全部选择"不改"。**

也就是说"观众感知不到歌在变"至少有三层原因：
1. 没刺激 → 0 决策（已证实）
2. **有刺激 → 决策了但选择不改**（新发现）
3. 改了但前端没暴露（你要做的三件事）

如果 ② 是常态，三件事做完观众**还是看不到变化**，会返工。所以建议开工前先花 10 分钟查一下 `noChange` 为什么 5/5——是策略太保守？还是我这条刺激 intensity 0.6 不够？还是轮次跑多了已经收敛？（我在生产打的，账本里能查到 `round:0:37`）

## 我建议补的一条（不在你三条里）

**现场必须有持续输入源。** 可视化是放大器，不是发生器——没人互动，歌就不会变，沉浸视图做得再漂亮也是张静态画面。眼镜掉线不等于蜂群停摆（观众触屏/挥手是免令牌真入口，一样能点着模型），但得**有人在点**。

建议在沉浸视图里给一个不说话的引导（不是文字解释音乐，是告诉手往哪放）：没人互动时萤火虫"等"的状态明显一点，或者观众页有个轻提示。**这条跟你"不加任何文字解释"不冲突。**

## 关于第三条（听觉记号）

支持，但建议**轻**：只在被改过的音上加一点点泛音，"略亮的音色"在 30 秒循环里容易显得脏。以你手机实听为准，拿不准就宁可更小——观众听不出来等于没做，但做重了整首歌会变难听。

---

## 补充（04:00，把代码翻完之后的修正——这条比上面更重要）

我把 `led-wall.js` / `music.mjs` / `music-audio.js` 翻了一遍，有件事得先说清楚，不然第一条做出来的画面是**假的**。

### 🔴 音符追不到"负责的蜂"——不是数据没给，是这个概念不成立

- 蜂的管辖是**滑动窗口**：`focusBeat ± 2 拍`（`src/music.mjs:553`）。49 只蜂的 focusBeat 均匀铺在 0–42.7 拍，**每个音符同时落在 4–5 只蜂的窗口里**。
- 所以我上一轮说"能 join"只对一半：`traces` 里有 `beeId`，能 join，但 join 出来的是「**本轮改它的那只蜂**」——**只有被改过的音符才有蜂，多数音符无蜂可亮**。
- 结论是：**"每个音符演奏的瞬间，负责它的那只蜂亮一下，整群萤火虫在唱这首歌"做不到**。硬做就只能按 focusBeat 挑"离得最近的那只蜂"，那是在观众眼前编造因果关系——这条我不建议做。

改成这样就不假了：
- **被本轮改过的音** → 那只蜂亮青白、更亮更久（这是唯一的、真实的因果）；
- **没有被改的音** → 不指派蜂。整群的呼吸跟着整体能量走就行，别冒充"是谁唱的"。

### 其他几条实测

- **三件事都不用改后端**：全在 `public/led-wall.js` 和 `public/music-audio.js`，合计 **140–210 行**，零 `src/` 改动、零规则改动。
- **"本轮改过"前端已经有现成判定**：`public/stage-math.js:154` 的 `changedInRound(note, roundId)`，detail 页已经在用，直接复用，不用加后端字段。
- **第 3 条（听觉记号）意外地干净**：`note.scheduled` 事件**本身就带了 `cause`**（`src/music.mjs:459`），所以只改 `music-audio.js` 三处、约 20–30 行就能给被改的音加泛音。
- **丝带会很空**：种子总共只有 **25 个音符、9 个不同 onset 拍位**（0.25/11/12/16/20/24/28/32/36/40），30 秒画"小刻度"大概就是稀稀拉拉几根。建议按 `voice`（pulse / chant-upper / chant-lower）分 2–3 条细轨，或者画带时值长度的条，别画成一排点。
- **一个现场风险**：`traces` 每轮 ×0.6 衰减，约 **27 轮后 join 会断**。本轮内没问题（strength=1 必在），但演出跑一整天之后，"很久以前被改、一路继承下来的音"可能查不到蜂——只影响细节页，不影响本轮青白高亮。

### 排期建议

**先做 2 + 3**（丝带 + 听觉记号，约 100–150 行，风险低、收益最直接）；**第 1 条降级为"被改的音符闪一下"**，别做"每只蜂负责一个音"。省下的时间拿去查 `noChange` 为什么 5/5——那个不解决，前两条做完观众看到的还是一片没变化的萤火虫。

---

## 可直接粘贴的回复（微信口吻）

> 做，但**第一条得改**，不然做出来是假的。
>
> 先说好消息：三件事**都不用改后端**，全在 `public/led-wall.js` + `public/music-audio.js`，合计 140–210 行。`traces[]` 里有独立的 `beeId`，不用解析 `observationId`（那串里还有个双写 r 的坑：`rround:0:37`，别解析它）。"本轮改过"前端已经有现成函数 `changedInRound()`（`stage-math.js:154`）直接用。第 3 条最省事——`note.scheduled` 事件本身就带 `cause`，改 3 处约 20–30 行就能给被改的音加泛音。
>
> **要改的是第一条**：蜂的管辖是 `focusBeat ± 2 拍`的滑动窗口，一个音符同时落在 **4–5 只蜂**的窗口里，而且只有被改过的音才有"蜂"。所以"每个音符响的时候负责它的那只蜂亮一下、整群萤火虫在唱这首歌"**做不到**——硬做只能按距离挑最近的那只，那是在观众眼前编因果关系。建议改成：**被本轮改过的音 → 那只蜂亮青白**（这是唯一真实的因果），**没被改的音不指派蜂**，整群呼吸跟着整体能量走就行。
>
> 顺带：种子只有 25 个音、9 个 onset 拍位，30 秒的"小刻度"丝带会很空，建议按 voice 分 2–3 条轨，或者画带时值长度的条。
>
> **还有一条可能让你返工**：蜂"决策了"不等于"改了"。我打刺激之前演出跑了 35 轮、0 决策；打进去之后 decisions 0→5，但那轮的 metrics 是 `decisions: 5 / noChange: 5 / appliedBees: 0`——**5 次决策全部选择不改**。建议开工前先花 10 分钟查下 `noChange` 为什么 5/5（账本 `round:0:37`）。
>
> 排期我建议**先做 2+3，第 1 条降级成"被改的音符闪一下"**，省下的时间拿去查 noChange。另外：**现场得有持续输入**——可视化是放大器不是发生器，没人互动歌就不会变。
>
> 听觉记号那条我支持，但轻一点，只加一点点泛音，做重了整首歌会变难听，以你手机实听为准。
